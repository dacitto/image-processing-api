import path from 'path';

export const fullImagesFolder = path.join(
  __dirname,
  '../../public/images/full/'
);

export const convertedImagesFolder = path.join(
  __dirname,
  '../../public/images/converted/'
);
